var loadNodeSubtreeByUUID = require('../../jseditor.js').loadNodeSubtreeByUUID;

describe( 'function loadNodeSubtreeByUUID', () => {
    beforeEach( () => {
        
    } );

    describe( 'uuid in cache', () => {
        it( 'should dosmth', () => {
            loadNodeSubtreeByUUID('');
        } );
    } );

    describe( 'uuid not in cache', () => {
        it( 'should dosmth when node is not `falsy`', () => {

        } );

        it( 'should dosmth when node is `falsy`', () => {

        } );
    } )

} );